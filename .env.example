# Database Configuration
# For Docker deployment, use: mongodb://mongodb:27017/idurar_db
# For local development, use: mongodb://localhost:27017/idurar_db
DATABASE=mongodb://mongodb:27017/idurar_db

# JWT Secret Key (REQUIRED - change this to a secure random string)
JWT_SECRET=your_private_jwt_secret_key_change_this_in_production

# Node Environment
NODE_ENV=production

# OpenSSL Configuration (for compatibility)
OPENSSL_CONF=/dev/null

# Public Server File URL
# For Docker deployment with frontend on port 8085, use: http://localhost:8085/
# For local development, use: http://localhost:8888/
PUBLIC_SERVER_FILE=http://localhost:8085/

# Email Service (Optional - Resend API)
# Get your API key from: https://resend.com
RESEND_API=your_resend_api_key

# OpenAI API Key (Optional - for AI features)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key

# Backend Port (Internal)
PORT=8888

# MongoDB Configuration (Optional - for custom MongoDB setup)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=changeme123
MONGO_INITDB_DATABASE=idurar_db
